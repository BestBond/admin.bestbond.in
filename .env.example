# Nest reward API (no trailing slash). Restart Vite after changes.
# Production build: set this to your public API URL before `npm run build` (value is baked into the bundle).
# Example: VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=http://localhost:3000
