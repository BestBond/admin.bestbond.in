# Admin SPA (Vite) — copy to .env.local for development.
# Production builds read .env.production (see .env.production.example).
# Never commit .env.local with secrets.

# --- API (required) ---
# No trailing slash. Local Nest default port is 3000.
VITE_API_URL=http://localhost:3000
